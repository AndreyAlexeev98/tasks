// №1
// Дана некоторая строка. Найдите позицию первого нуля в строке.
function findZero(string) {
  return string.indexOf("0");
}

// №2
// Выведите в консоль все числа в промежутке от 1 до 1000, сумма первой и второй цифры которых равна пяти.
function outputNumbers() {
  for (let i = 1; i < 1000; i++) {
    const numbersStringType = i.toString();

    if (
      +numbersStringType[0] +
        +numbersStringType[numbersStringType.length - 1] ===
      5
    ) {
      console.log(i);
    }
  }
}

// №3
// Дан массив. Удалите из него элементы с заданным значением. Удаление необходимо произвести (на месте), т.е. с мутацией (изменением) входного массива.
function removeElems(array, ...rest) {
  // Решение №1 - наихудшая производительность
  // Сложность: 𝑂(𝑛×𝑚), где 𝑛 — длина массива array, 𝑚 — длина массива rest.
  // Причины низкой производительности:
  // - Два вложенных цикла.
  // - Каждый вызов splice изменяет массив, что требует смещения всех последующих элементов, что добавляет накладные расходы.
  // - Особенно затратен для больших массивов.

  //   for (let i = 0; i < array.length; i++) {
  //     for (let j = 0; j < rest.length; j++) {
  //       if (array[i] === rest[j]) {
  //         array.splice(i, 1);
  //         i--;
  //       }
  //     }
  //   }

  // Решение №2 Средняя производительность
  // Сложность: 𝑂(𝑛×𝑚)
  // Почему производительность выше, чем у предыдущего варианта:
  // - Убрана вложенность циклов, но вызов rest.includes на каждой итерации цикла создает ту же сложность.
  // - Как и раньше, splice добавляет накладные расходы на изменение массива.

  //   let i = 0;
  //   while (i < array.length) {
  //     if (rest.includes(array[i])) {
  //       array.splice(i, 1);
  //     } else {
  //       i++;
  //     }
  //   }

  // Решение №3 Средняя производительность
  // - Сложность: 𝑂(𝑛×𝑚)
  // - Производительность:
  // - Сложность схожа с предыдущим вариантом из-за вызова rest.includes на каждую итерацию.
  // - Не является самым производительным, если учитывать реальное выполнение с большими массивами.

  //   array = array.filter((item) => !rest.include(item));

  // Решение №4 Наилучшая производительность
  //   Сложность: 𝑂(𝑛+𝑚)
  // Причины производительности выше:
  // - Конвертация rest в Set (𝑂(𝑚)) позволяет выполнять проверки через Set.has за 𝑂(1).
  // - splice все еще создает накладные расходы на изменение массива, но сама проверка элементов работает гораздо быстрее.

  let valuesToRemove = new Set(rest);
  let i = 0;

  while (i < array.length) {
    if (valuesToRemove.has(array[i])) {
      array.splice(i, 1);
    } else {
      i++;
    }
  }
}

// №4
// Дан некоторый массив, например, вот такой: [1, 2, 3, 4, 5, 6]
// Найдите сумму первой половины элементов этого массива.

function sumHalf(arr) {
  const halfArr = arr.slice(0, Math.floor(arr.length / 2));
  return halfArr.reduce((sum, num) => (sum += num), 0);

  // Или по классике
  //   const sum = 0;
  //   for (let i = 0; i < halfArr.length; i++) {
  //     sum += halfArr[i];
  //   }

  //   return sum;
}
